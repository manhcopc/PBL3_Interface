<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
  x:Class="PBL3_Interface.Pages.ShiftPage"
  xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
  xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
  xmlns:local="clr-namespace:PBL3_Interface.Pages;assembly=PBL3_Interface"
  BackgroundColor="#F7EFE9"
  ControlTemplate="{StaticResource SharedManagerPageTemplate}">
  <Grid Margin="100,0,100,100" RowDefinitions="auto,*">
    <Label
      Grid.Row="0"
      Margin="0,50,0,50"
      FontAttributes="Bold"
      FontSize="40"
      HorizontalOptions="Center"
      Text="QUẢN LÝ CA LÀM VIỆC"
      TextColor="Black"
      VerticalOptions="End" />
    <!--  Grid chính  -->
    <Frame
      Grid.Row="1"
      BackgroundColor="Transparent"
      BorderColor="Gray"
      HasShadow="False">
      <Grid
        Padding="20"
        ColumnDefinitions="3*, *"
        ColumnSpacing="20">
        <!--  Phần 1: Hiển thị ca làm (bên trái)  -->
        <ScrollView Grid.Column="0">
          <Frame
            Padding="15"
            BackgroundColor="#FFECEC"
            BorderColor="#CCCCCC"
            CornerRadius="10"
            HorizontalOptions="Center"
            VerticalOptions="Start">

            <VerticalStackLayout Spacing="15">
              <!--  Ca 1  -->
              <Frame
                Padding="10"
                BackgroundColor="Pink"
                BorderColor="Transparent"
                CornerRadius="5">
                <Grid ColumnDefinitions="3*,*">
                  <Frame
                    Grid.Column="0"
                    Margin="0,0,0,5"
                    Padding="10"
                    BackgroundColor="Transparent"
                    BorderColor="Transparent"
                    CornerRadius="5">
                    <Frame.GestureRecognizers>
                      <TapGestureRecognizer Command="{Binding ToggleShift1Command}" />
                    </Frame.GestureRecognizers>
                    <Label
                      FontAttributes="Bold"
                      FontSize="20"
                      HorizontalOptions="Start"
                      Text="Ca 1"
                      TextColor="Black" />
                  </Frame>
                  <Button
                    Grid.Column="1"
                    Padding="10,5"
                    BackgroundColor="#E5E5E5"
                    Command="{Binding AddEmployeeToShift1Command}"
                    CornerRadius="5"
                    FontSize="14"
                    Text="Thêm nhân viên"
                    VerticalOptions="Center" />
                </Grid>
              </Frame>
              <VerticalStackLayout IsVisible="{Binding IsShift1Visible}" Spacing="10">
                <!--  Vai trò: Thu ngân  -->
                <Label
                  Margin="0,0,0,5"
                  FontAttributes="Bold"
                  FontSize="18"
                  Text="Thu ngân"
                  TextColor="Black" />
                <CollectionView
                  Margin="0,0,0,5"
                  BackgroundColor="Transparent"
                  ItemsSource="{Binding Shift1Cashiers}">
                  <CollectionView.ItemTemplate>
                    <DataTemplate>
                      <Frame
                        Margin="0,0,0,5"
                        Padding="10"
                        BackgroundColor="#F8E0E0"
                        BorderColor="Transparent"
                        CornerRadius="5">
                        <Grid ColumnDefinitions="2*, *, *, *, Auto" ColumnSpacing="10">
                          <Label
                            Grid.Column="0"
                            FontSize="16"
                            Text="{Binding Name}"
                            TextColor="Black"
                            VerticalOptions="Center" />
                          <Label
                            Grid.Column="1"
                            FontSize="16"
                            Text="{Binding DOB}"
                            TextColor="Black"
                            VerticalOptions="Center" />
                          <Label
                            Grid.Column="2"
                            FontSize="16"
                            Text="{Binding PhoneNumber}"
                            TextColor="Black"
                            VerticalOptions="Center" />
                          <Label
                            Grid.Column="3"
                            FontSize="16"
                            Text="{Binding Role}"
                            TextColor="Black"
                            VerticalOptions="Center" />
                          <Button
                            Grid.Column="4"
                            Padding="5"
                            BackgroundColor="Red"
                            Command="{Binding Source={RelativeSource AncestorType={x:Type local:ShiftPageViewModel}}, Path=RemoveShift1CashierCommand}"
                            CommandParameter="{Binding .}"
                            CornerRadius="5"
                            FontSize="14"
                            Text="Xóa"
                            TextColor="White" />
                        </Grid>
                      </Frame>
                    </DataTemplate>
                  </CollectionView.ItemTemplate>
                </CollectionView>

                <!--  Vai trò: Pha chế  -->
                <Label
                  Margin="0,0,0,5"
                  FontAttributes="Bold"
                  FontSize="18"
                  Text="Pha chế"
                  TextColor="Black" />
                <CollectionView
                  Margin="0,0,0,5"
                  BackgroundColor="Transparent"
                  ItemsSource="{Binding Shift1Baristas}">
                  <CollectionView.ItemTemplate>
                    <DataTemplate>
                      <Frame
                        Margin="0,0,0,5"
                        Padding="10"
                        BackgroundColor="#F8E0E0"
                        BorderColor="Transparent"
                        CornerRadius="5">
                        <Grid ColumnDefinitions="2*, *, *, *, Auto" ColumnSpacing="10">
                          <Label
                            Grid.Column="0"
                            FontSize="16"
                            Text="{Binding Name}"
                            TextColor="Black"
                            VerticalOptions="Center" />
                          <Label
                            Grid.Column="1"
                            FontSize="16"
                            Text="{Binding DOB}"
                            TextColor="Black"
                            VerticalOptions="Center" />
                          <Label
                            Grid.Column="2"
                            FontSize="16"
                            Text="{Binding PhoneNumber}"
                            TextColor="Black"
                            VerticalOptions="Center" />
                          <Label
                            Grid.Column="3"
                            FontSize="16"
                            Text="{Binding Role}"
                            TextColor="Black"
                            VerticalOptions="Center" />
                          <Button
                            Grid.Column="4"
                            Padding="5"
                            BackgroundColor="Red"
                            Command="{Binding Source={RelativeSource AncestorType={x:Type local:ShiftPageViewModel}}, Path=RemoveShift1BaristaCommand}"
                            CommandParameter="{Binding .}"
                            CornerRadius="5"
                            FontSize="14"
                            Text="Xóa"
                            TextColor="White" />
                        </Grid>
                      </Frame>
                    </DataTemplate>
                  </CollectionView.ItemTemplate>
                </CollectionView>

                <!--  Vai trò: Phục vụ  -->
                <Label
                  Margin="0,0,0,5"
                  FontAttributes="Bold"
                  FontSize="18"
                  Text="Phục vụ"
                  TextColor="Black" />
                <CollectionView
                  Margin="0,0,0,5"
                  BackgroundColor="Transparent"
                  ItemsSource="{Binding Shift1Waiters}">
                  <CollectionView.ItemTemplate>
                    <DataTemplate>
                      <Frame
                        Margin="0,0,0,5"
                        Padding="10"
                        BackgroundColor="#F8E0E0"
                        BorderColor="Transparent"
                        CornerRadius="5">
                        <Grid ColumnDefinitions="2*, *, *, *, Auto" ColumnSpacing="10">
                          <Label
                            Grid.Column="0"
                            FontSize="16"
                            Text="{Binding Name}"
                            TextColor="Black"
                            VerticalOptions="Center" />
                          <Label
                            Grid.Column="1"
                            FontSize="16"
                            Text="{Binding DOB}"
                            TextColor="Black"
                            VerticalOptions="Center" />
                          <Label
                            Grid.Column="2"
                            FontSize="16"
                            Text="{Binding PhoneNumber}"
                            TextColor="Black"
                            VerticalOptions="Center" />
                          <Label
                            Grid.Column="3"
                            FontSize="16"
                            Text="{Binding Role}"
                            TextColor="Black"
                            VerticalOptions="Center" />
                          <Button
                            Grid.Column="4"
                            Padding="5"
                            BackgroundColor="Red"
                            Command="{Binding Source={RelativeSource AncestorType={x:Type local:ShiftPageViewModel}}, Path=RemoveShift1WaiterCommand}"
                            CommandParameter="{Binding .}"
                            CornerRadius="5"
                            FontSize="14"
                            Text="Xóa"
                            TextColor="White" />
                        </Grid>
                      </Frame>
                    </DataTemplate>
                  </CollectionView.ItemTemplate>
                </CollectionView>
              </VerticalStackLayout>

              <!--  Ca 2  -->
              <Frame
                Padding="10"
                BackgroundColor="Pink"
                BorderColor="Transparent"
                CornerRadius="5">
                <Grid ColumnDefinitions="3*,*">
                  <Frame
                    Grid.Column="0"
                    Margin="0,0,0,5"
                    Padding="10"
                    BackgroundColor="Pink"
                    BorderColor="Transparent"
                    CornerRadius="5">
                    <Frame.GestureRecognizers>
                      <TapGestureRecognizer Command="{Binding ToggleShift2Command}" />
                    </Frame.GestureRecognizers>
                    <Label
                      FontAttributes="Bold"
                      FontSize="20"
                      HorizontalOptions="Start"
                      Text="Ca 2"
                      TextColor="Black" />
                  </Frame>
                  <Button
                    Grid.Column="1"
                    Padding="10,5"
                    BackgroundColor="#E5E5E5"
                    Command="{Binding AddEmployeeToShift2Command}"
                    CornerRadius="5"
                    FontSize="14"
                    Text="Thêm nhân viên"
                    VerticalOptions="Center" />
                </Grid>
              </Frame>
              <VerticalStackLayout IsVisible="{Binding IsShift2Visible}" Spacing="10">
                <!--  Vai trò: Thu ngân  -->
                <Label
                  Margin="0,0,0,5"
                  FontAttributes="Bold"
                  FontSize="18"
                  Text="Thu ngân"
                  TextColor="Black" />
                <CollectionView
                  Margin="0,0,0,5"
                  BackgroundColor="Transparent"
                  ItemsSource="{Binding Shift2Cashiers}">
                  <CollectionView.ItemTemplate>
                    <DataTemplate>
                      <Frame
                        Margin="0,0,0,5"
                        Padding="10"
                        BackgroundColor="#F8E0E0"
                        BorderColor="Transparent"
                        CornerRadius="5">
                        <Grid ColumnDefinitions="2*, *, *, *, Auto" ColumnSpacing="10">
                          <Label
                            Grid.Column="0"
                            FontSize="16"
                            Text="{Binding Name}"
                            TextColor="Black"
                            VerticalOptions="Center" />
                          <Label
                            Grid.Column="1"
                            FontSize="16"
                            Text="{Binding DOB}"
                            TextColor="Black"
                            VerticalOptions="Center" />
                          <Label
                            Grid.Column="2"
                            FontSize="16"
                            Text="{Binding PhoneNumber}"
                            TextColor="Black"
                            VerticalOptions="Center" />
                          <Label
                            Grid.Column="3"
                            FontSize="16"
                            Text="{Binding Role}"
                            TextColor="Black"
                            VerticalOptions="Center" />
                          <Button
                            Grid.Column="4"
                            Padding="5"
                            BackgroundColor="Red"
                            Command="{Binding Source={RelativeSource AncestorType={x:Type local:ShiftPageViewModel}}, Path=RemoveShift2CashierCommand}"
                            CommandParameter="{Binding .}"
                            CornerRadius="5"
                            FontSize="14"
                            Text="Xóa"
                            TextColor="White" />
                        </Grid>
                      </Frame>
                    </DataTemplate>
                  </CollectionView.ItemTemplate>
                </CollectionView>

                <!--  Vai trò: Pha chế  -->
                <Label
                  Margin="0,0,0,5"
                  FontAttributes="Bold"
                  FontSize="18"
                  Text="Pha chế"
                  TextColor="Black" />
                <CollectionView
                  Margin="0,0,0,5"
                  BackgroundColor="Transparent"
                  ItemsSource="{Binding Shift2Baristas}">
                  <CollectionView.ItemTemplate>
                    <DataTemplate>
                      <Frame
                        Margin="0,0,0,5"
                        Padding="10"
                        BackgroundColor="#F8E0E0"
                        BorderColor="Transparent"
                        CornerRadius="5">
                        <Grid ColumnDefinitions="2*, *, *, *, Auto" ColumnSpacing="10">
                          <Label
                            Grid.Column="0"
                            FontSize="16"
                            Text="{Binding Name}"
                            TextColor="Black"
                            VerticalOptions="Center" />
                          <Label
                            Grid.Column="1"
                            FontSize="16"
                            Text="{Binding DOB}"
                            TextColor="Black"
                            VerticalOptions="Center" />
                          <Label
                            Grid.Column="2"
                            FontSize="16"
                            Text="{Binding PhoneNumber}"
                            TextColor="Black"
                            VerticalOptions="Center" />
                          <Label
                            Grid.Column="3"
                            FontSize="16"
                            Text="{Binding Role}"
                            TextColor="Black"
                            VerticalOptions="Center" />
                          <Button
                            Grid.Column="4"
                            Padding="5"
                            BackgroundColor="Red"
                            Command="{Binding Source={RelativeSource AncestorType={x:Type local:ShiftPageViewModel}}, Path=RemoveShift2BaristaCommand}"
                            CommandParameter="{Binding .}"
                            CornerRadius="5"
                            FontSize="14"
                            Text="Xóa"
                            TextColor="White" />
                        </Grid>
                      </Frame>
                    </DataTemplate>
                  </CollectionView.ItemTemplate>
                </CollectionView>

                <!--  Vai trò: Phục vụ  -->
                <Label
                  Margin="0,0,0,5"
                  FontAttributes="Bold"
                  FontSize="18"
                  Text="Phục vụ"
                  TextColor="Black" />
                <CollectionView
                  Margin="0,0,0,5"
                  BackgroundColor="Transparent"
                  ItemsSource="{Binding Shift2Waiters}">
                  <CollectionView.ItemTemplate>
                    <DataTemplate>
                      <Frame
                        Margin="0,0,0,5"
                        Padding="10"
                        BackgroundColor="#F8E0E0"
                        BorderColor="Transparent"
                        CornerRadius="5">
                        <Grid ColumnDefinitions="2*, *, *, *, Auto" ColumnSpacing="10">
                          <Label
                            Grid.Column="0"
                            FontSize="16"
                            Text="{Binding Name}"
                            TextColor="Black"
                            VerticalOptions="Center" />
                          <Label
                            Grid.Column="1"
                            FontSize="16"
                            Text="{Binding DOB}"
                            TextColor="Black"
                            VerticalOptions="Center" />
                          <Label
                            Grid.Column="2"
                            FontSize="16"
                            Text="{Binding PhoneNumber}"
                            TextColor="Black"
                            VerticalOptions="Center" />
                          <Label
                            Grid.Column="3"
                            FontSize="16"
                            Text="{Binding Role}"
                            TextColor="Black"
                            VerticalOptions="Center" />
                          <Button
                            Grid.Column="4"
                            Padding="5"
                            BackgroundColor="Red"
                            Command="{Binding Source={RelativeSource AncestorType={x:Type local:ShiftPageViewModel}}, Path=RemoveShift2WaiterCommand}"
                            CommandParameter="{Binding .}"
                            CornerRadius="5"
                            FontSize="14"
                            Text="Xóa"
                            TextColor="White" />
                        </Grid>
                      </Frame>
                    </DataTemplate>
                  </CollectionView.ItemTemplate>
                </CollectionView>
              </VerticalStackLayout>

              <!--  Ca 3  -->
              <Frame
                Padding="10"
                BackgroundColor="Pink"
                BorderColor="Transparent"
                CornerRadius="5">
                <Grid ColumnDefinitions="3*,*">
                  <Frame
                    Grid.Column="0"
                    Margin="0,0,0,5"
                    Padding="10"
                    BackgroundColor="Pink"
                    BorderColor="Transparent"
                    CornerRadius="5">
                    <Frame.GestureRecognizers>
                      <TapGestureRecognizer Command="{Binding ToggleShift3Command}" />
                    </Frame.GestureRecognizers>
                    <Label
                      FontAttributes="Bold"
                      FontSize="20"
                      HorizontalOptions="Start"
                      Text="Ca 3"
                      TextColor="Black" />
                  </Frame>
                  <Button
                    Grid.Column="1"
                    Padding="10,5"
                    BackgroundColor="#E5E5E5"
                    Command="{Binding AddEmployeeToShift3Command}"
                    CornerRadius="5"
                    FontSize="14"
                    Text="Thêm nhân viên"
                    VerticalOptions="Center" />
                </Grid>
              </Frame>
              <VerticalStackLayout IsVisible="{Binding IsShift3Visible}" Spacing="10">
                <!--  Vai trò: Thu ngân  -->
                <Label
                  Margin="0,0,0,5"
                  FontAttributes="Bold"
                  FontSize="18"
                  Text="Thu ngân"
                  TextColor="Black" />
                <CollectionView
                  Margin="0,0,0,5"
                  BackgroundColor="Transparent"
                  ItemsSource="{Binding Shift3Cashiers}">
                  <CollectionView.ItemTemplate>
                    <DataTemplate>
                      <Frame
                        Margin="0,0,0,5"
                        Padding="10"
                        BackgroundColor="#F8E0E0"
                        BorderColor="Transparent"
                        CornerRadius="5">
                        <Grid ColumnDefinitions="2*, *, *, *, Auto" ColumnSpacing="10">
                          <Label
                            Grid.Column="0"
                            FontSize="16"
                            Text="{Binding Name}"
                            TextColor="Black"
                            VerticalOptions="Center" />
                          <Label
                            Grid.Column="1"
                            FontSize="16"
                            Text="{Binding DOB}"
                            TextColor="Black"
                            VerticalOptions="Center" />
                          <Label
                            Grid.Column="2"
                            FontSize="16"
                            Text="{Binding PhoneNumber}"
                            TextColor="Black"
                            VerticalOptions="Center" />
                          <Label
                            Grid.Column="3"
                            FontSize="16"
                            Text="{Binding Role}"
                            TextColor="Black"
                            VerticalOptions="Center" />
                          <Button
                            Grid.Column="4"
                            Padding="5"
                            BackgroundColor="Red"
                            Command="{Binding Source={RelativeSource AncestorType={x:Type local:ShiftPageViewModel}}, Path=RemoveShift3CashierCommand}"
                            CommandParameter="{Binding .}"
                            CornerRadius="5"
                            FontSize="14"
                            Text="Xóa"
                            TextColor="White" />
                        </Grid>
                      </Frame>
                    </DataTemplate>
                  </CollectionView.ItemTemplate>
                </CollectionView>

                <!--  Vai trò: Pha chế  -->
                <Label
                  Margin="0,0,0,5"
                  FontAttributes="Bold"
                  FontSize="18"
                  Text="Pha chế"
                  TextColor="Black" />
                <CollectionView
                  Margin="0,0,0,5"
                  BackgroundColor="Transparent"
                  ItemsSource="{Binding Shift3Baristas}">
                  <CollectionView.ItemTemplate>
                    <DataTemplate>
                      <Frame
                        Margin="0,0,0,5"
                        Padding="10"
                        BackgroundColor="#F8E0E0"
                        BorderColor="Transparent"
                        CornerRadius="5">
                        <Grid ColumnDefinitions="2*, *, *, *, Auto" ColumnSpacing="10">
                          <Label
                            Grid.Column="0"
                            FontSize="16"
                            Text="{Binding Name}"
                            TextColor="Black"
                            VerticalOptions="Center" />
                          <Label
                            Grid.Column="1"
                            FontSize="16"
                            Text="{Binding DOB}"
                            TextColor="Black"
                            VerticalOptions="Center" />
                          <Label
                            Grid.Column="2"
                            FontSize="16"
                            Text="{Binding PhoneNumber}"
                            TextColor="Black"
                            VerticalOptions="Center" />
                          <Label
                            Grid.Column="3"
                            FontSize="16"
                            Text="{Binding Role}"
                            TextColor="Black"
                            VerticalOptions="Center" />
                          <Button
                            Grid.Column="4"
                            Padding="5"
                            BackgroundColor="Red"
                            Command="{Binding Source={RelativeSource AncestorType={x:Type local:ShiftPageViewModel}}, Path=RemoveShift3BaristaCommand}"
                            CommandParameter="{Binding .}"
                            CornerRadius="5"
                            FontSize="14"
                            Text="Xóa"
                            TextColor="White" />
                        </Grid>
                      </Frame>
                    </DataTemplate>
                  </CollectionView.ItemTemplate>
                </CollectionView>

                <!--  Vai trò: Phục vụ  -->
                <Label
                  Margin="0,0,0,5"
                  FontAttributes="Bold"
                  FontSize="18"
                  Text="Phục vụ"
                  TextColor="Black" />
                <CollectionView
                  Margin="0,0,0,5"
                  BackgroundColor="Transparent"
                  ItemsSource="{Binding Shift3Waiters}">
                  <CollectionView.ItemTemplate>
                    <DataTemplate>
                      <Frame
                        Margin="0,0,0,5"
                        Padding="10"
                        BackgroundColor="#F8E0E0"
                        BorderColor="Transparent"
                        CornerRadius="5">
                        <Grid ColumnDefinitions="2*, *, *, *, Auto" ColumnSpacing="10">
                          <Label
                            Grid.Column="0"
                            FontSize="16"
                            Text="{Binding Name}"
                            TextColor="Black"
                            VerticalOptions="Center" />
                          <Label
                            Grid.Column="1"
                            FontSize="16"
                            Text="{Binding DOB}"
                            TextColor="Black"
                            VerticalOptions="Center" />
                          <Label
                            Grid.Column="2"
                            FontSize="16"
                            Text="{Binding PhoneNumber}"
                            TextColor="Black"
                            VerticalOptions="Center" />
                          <Label
                            Grid.Column="3"
                            FontSize="16"
                            Text="{Binding Role}"
                            TextColor="Black"
                            VerticalOptions="Center" />
                          <Button
                            Grid.Column="4"
                            Padding="5"
                            BackgroundColor="Red"
                            Command="{Binding Source={RelativeSource AncestorType={x:Type local:ShiftPageViewModel}}, Path=RemoveShift3WaiterCommand}"
                            CommandParameter="{Binding .}"
                            CornerRadius="5"
                            FontSize="14"
                            Text="Xóa"
                            TextColor="White" />
                        </Grid>
                      </Frame>
                    </DataTemplate>
                  </CollectionView.ItemTemplate>
                </CollectionView>
              </VerticalStackLayout>
              <!--  Ngày làm  -->
              <Grid ColumnDefinitions="*,auto">
                <ScrollView
                  Grid.Column="0"
                  BackgroundColor="Transparent"
                  Orientation="Horizontal">
                  <HorizontalStackLayout Margin="0,10,0,0" Spacing="10">
                    <Button
                      Padding="10,5"
                      BackgroundColor="{Binding DateButtonBackground1}"
                      Command="{Binding SelectDateCommand}"
                      CommandParameter="01/04/2025"
                      CornerRadius="20"
                      FontSize="14"
                      Text="01/04"
                      TextColor="White" />
                    <Button
                      Padding="10,5"
                      BackgroundColor="{Binding DateButtonBackground2}"
                      Command="{Binding SelectDateCommand}"
                      CommandParameter="02/04/2025"
                      CornerRadius="20"
                      FontSize="14"
                      Text="02/04"
                      TextColor="White" />
                    <Button
                      Padding="10,5"
                      BackgroundColor="{Binding DateButtonBackground3}"
                      Command="{Binding SelectDateCommand}"
                      CommandParameter="03/04/2025"
                      CornerRadius="20"
                      FontSize="14"
                      Text="03/04"
                      TextColor="White" />
                    <Button
                      Padding="10,5"
                      BackgroundColor="{Binding DateButtonBackground4}"
                      Command="{Binding SelectDateCommand}"
                      CommandParameter="04/04/2025"
                      CornerRadius="20"
                      FontSize="14"
                      Text="04/04"
                      TextColor="White" />
                    <Button
                      Padding="10,5"
                      BackgroundColor="{Binding DateButtonBackground5}"
                      Command="{Binding SelectDateCommand}"
                      CommandParameter="05/04/2025"
                      CornerRadius="20"
                      FontSize="14"
                      Text="05/04"
                      TextColor="White" />
                    <Button
                      Padding="10,5"
                      BackgroundColor="{Binding DateButtonBackground6}"
                      Command="{Binding SelectDateCommand}"
                      CommandParameter="06/04/2025"
                      CornerRadius="20"
                      FontSize="14"
                      Text="06/04"
                      TextColor="White" />
                    <Button
                      Padding="10,5"
                      BackgroundColor="{Binding DateButtonBackground7}"
                      Command="{Binding SelectDateCommand}"
                      CommandParameter="07/04/2025"
                      CornerRadius="20"
                      FontSize="14"
                      Text="07/04"
                      TextColor="White" />
                    <Button
                      Padding="10,5"
                      BackgroundColor="{Binding DateButtonBackground8}"
                      Command="{Binding SelectDateCommand}"
                      CommandParameter="08/04/2025"
                      CornerRadius="20"
                      FontSize="14"
                      Text="08/04"
                      TextColor="White" />
                    <Button
                      Padding="10,5"
                      BackgroundColor="{Binding DateButtonBackground9}"
                      Command="{Binding SelectDateCommand}"
                      CommandParameter="09/04/2025"
                      CornerRadius="20"
                      FontSize="14"
                      Text="09/04"
                      TextColor="White" />
                    <Button
                      Padding="10,5"
                      BackgroundColor="{Binding DateButtonBackground10}"
                      Command="{Binding SelectDateCommand}"
                      CommandParameter="10/04/2025"
                      CornerRadius="20"
                      FontSize="14"
                      Text="10/04"
                      TextColor="White" />
                  </HorizontalStackLayout>
                </ScrollView>
                <Button
                  Grid.Column="1"
                  Padding="10,5"
                  BackgroundColor="#E5E5E5"
                  Command="{Binding AddDateCommand}"
                  CornerRadius="20"
                  FontSize="14"
                  Text="Thêm ngày làm"
                  TextColor="Black" />
              </Grid>
            </VerticalStackLayout>
          </Frame>
        </ScrollView>

        <!--  Phần 2: Thống kê chi tiết ca làm (bên phải)  -->
        <ScrollView Grid.Column="1">
          <VerticalStackLayout Spacing="15" VerticalOptions="Start">
            <Label
              FontAttributes="Bold"
              FontSize="20"
              HorizontalOptions="Start"
              Text="{Binding SelectedDate, StringFormat='Ngày làm: {0}'}"
              TextColor="Black" />
            <Label
              Margin="0,10,0,5"
              FontAttributes="Bold"
              FontSize="18"
              Text="Tổng nhân viên:"
              TextColor="Black" />
            <Label
              Margin="10,0,0,0"
              FontSize="16"
              Text="{Binding TotalEmployees, StringFormat='Ca 1: {0}'}"
              TextColor="Black" />
            <Label
              Margin="10,0,0,0"
              FontSize="16"
              Text="{Binding TotalEmployeesShift2, StringFormat='Ca 2: {0}'}"
              TextColor="Black" />
            <Label
              Margin="10,0,0,0"
              FontSize="16"
              Text="{Binding TotalEmployeesShift3, StringFormat='Ca 3: {0}'}"
              TextColor="Black" />
            <Label
              Margin="0,10,0,5"
              FontAttributes="Bold"
              FontSize="18"
              Text="Tổng nhân viên theo vị trí:"
              TextColor="Black" />
            <!--  Ca 1  -->
            <Label
              Margin="10,0,0,0"
              FontAttributes="Bold"
              FontSize="16"
              Text="Ca 1:"
              TextColor="Black" />
            <Label
              Margin="20,0,0,0"
              FontSize="16"
              Text="{Binding Shift1CashierCount, StringFormat='Thu ngân: {0}'}"
              TextColor="Black" />
            <Label
              Margin="20,0,0,0"
              FontSize="16"
              Text="{Binding Shift1BaristaCount, StringFormat='Pha chế: {0}'}"
              TextColor="Black" />
            <Label
              Margin="20,0,0,0"
              FontSize="16"
              Text="{Binding Shift1WaiterCount, StringFormat='Phục vụ: {0}'}"
              TextColor="Black" />
            <!--  Ca 2  -->
            <Label
              Margin="10,0,0,0"
              FontAttributes="Bold"
              FontSize="16"
              Text="Ca 2:"
              TextColor="Black" />
            <Label
              Margin="20,0,0,0"
              FontSize="16"
              Text="{Binding Shift2CashierCount, StringFormat='Thu ngân: {0}'}"
              TextColor="Black" />
            <Label
              Margin="20,0,0,0"
              FontSize="16"
              Text="{Binding Shift2BaristaCount, StringFormat='Pha chế: {0}'}"
              TextColor="Black" />
            <Label
              Margin="20,0,0,0"
              FontSize="16"
              Text="{Binding Shift2WaiterCount, StringFormat='Phục vụ: {0}'}"
              TextColor="Black" />
            <!--  Ca 3  -->
            <Label
              Margin="10,0,0,0"
              FontAttributes="Bold"
              FontSize="16"
              Text="Ca 3:"
              TextColor="Black" />
            <Label
              Margin="20,0,0,0"
              FontSize="16"
              Text="{Binding Shift3CashierCount, StringFormat='Thu ngân: {0}'}"
              TextColor="Black" />
            <Label
              Margin="20,0,0,0"
              FontSize="16"
              Text="{Binding Shift3BaristaCount, StringFormat='Pha chế: {0}'}"
              TextColor="Black" />
            <Label
              Margin="20,0,0,0"
              FontSize="16"
              Text="{Binding Shift3WaiterCount, StringFormat='Phục vụ: {0}'}"
              TextColor="Black" />
          </VerticalStackLayout>
        </ScrollView>
      </Grid>
    </Frame>
  </Grid>
</ContentPage>
